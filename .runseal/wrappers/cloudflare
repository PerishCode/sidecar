#!/usr/bin/env sh
set -eu

case "${1:-}" in
  -h|--help|help)
    cat <<'EOF'
sidecar Cloudflare support placeholder

Usage:
  runseal :cloudflare

The sidecar.perish.uk mapping workflow is intentionally left as a placeholder.
Provide the Cloudflare token and desired mapping details before implementing it.
EOF
    exit 0
    ;;
esac

cat >&2 <<'EOF'
sidecar.perish.uk Cloudflare mapping is not implemented yet.
Provide the Cloudflare token and mapping details before this wrapper performs changes.
EOF
exit 2
